Experiment 3.4 LEETCODE 577


Task : Write a solution to report the name and bonus amount of each employee who satisfies either of the following:

The employee has a bonus less than 1000.
The employee did not get any bonus.
Return the result table in any order.

The result format is in the following example.

Code : select e.name , b.bonus from Employee as e LEFT JOIN Bonus as b on e.empID = b.empID where b.bonus<1000 or b.bonus is NULL;